version: 2
formats:
  - htmlzip
  - pdf

python:
  version: 3.6
  install:
    - method: pip
      path: .
  setup_py_install: true
conda:
  environment: readthedocs-environment.yml

build:
  image: latest
